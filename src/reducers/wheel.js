

const initialState = {
  draw: false,
  started: false,
  letterBoard: "-letterboardtest--",
  word: "quinoa",
  guesses: [],
  completed: false,
  wheelValue: 10,

}

export default (state = initialState, { type, payload } = {}) => {

      return state
}
