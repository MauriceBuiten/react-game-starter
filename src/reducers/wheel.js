

const initialState = {
  letterBoard: "-letterboardtest--",
  word: "quinoa",
  guesses: ['a'],
  completed: false,
  wheelValue: 10,

}

export default (state = initialState, { type, payload } = {}) => {

      return state
}
